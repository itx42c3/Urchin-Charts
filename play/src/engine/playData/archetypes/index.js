import { Initialization } from './Initialization.js'
import { Stage } from './Stage.js'
import { TapNote } from './TapNote.js' // これを新しく作ります

export const archetypes = defineArchetypes({
    Initialization,
    Stage,
    TapNote, // 追加
})
